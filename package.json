{
  "name": "legal-genie",
  "version": "1.0.0",
  "private": true,
  "description": "Legal Genie — legal department management system (cases, contracts, documents, tasks, approvals).",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server.js",
    "test": "SUPABASE_URL=http://local-mock SUPABASE_SERVICE_ROLE_KEY=test-only node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.9"
  }
}
