services:
  - type: web
    runtime: node
    name: legal-genie
    plan: starter
    startCommand: node server.js
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /data
      - key: NODE_VERSION
        value: 22.13.0
